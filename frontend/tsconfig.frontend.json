///frontend/tsconfig.frontend.json
{
  "extends": "../tsconfig.base.json", // 共通設定を継承
  "compilerOptions": {
    "jsx": "react-jsx", // ReactのJSXサポートを有効にする
    "lib": ["dom", "dom.iterable", "esnext"], // ブラウザ向けのAPIや最新のJS機能を使用
    "rootDir": "src", // ソースファイルのルートディレクトリ
    "baseUrl": "src",
    "module": "esnext"
  },

  "include": ["src/**/*"], // フロントエンドのソースコードを対象
  "exclude": ["node_modules", "build"] // node_modulesとbuildディレクトリを除外
}
